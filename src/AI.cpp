#include "AI.h"

using namespace std;

/* Class variables
 * 		std::vector<int> score;
 *		std::vector<int> top_tiles;
 *		int nb_players;
 *		std::set<Tile> tiles;
 * 		int id;
 *		int score;
 *		std::string name;
 *		std::stack<Tile> tiles;
 *		std::multiset<Dice> dice;
 */


void turn_start() {
	
}

/* 	
 * 	@POST	returns -1 to roll the dice
 * 			returns [21;36] to take a tile
 */
int roll_or_stop() {
	
	
}

/*	
 * 	@PRE	multiset of the dice that has been rolled
 * 	@POST	returns die value the AI wants to take
 */
int choose_dice(std::multiset<Dice> roll) {
	
}

void turn_end() {
	
}
